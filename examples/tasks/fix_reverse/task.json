{
  "task_id": "fix_reverse_001",
  "description": "The function reverse_string in reverse.py is broken. Fix it so all tests pass. It should return the input string reversed.",
  "starter_files": {
    "reverse.py": "def reverse_string(s):\n    return s\n",
    "test_reverse.py": "from reverse import reverse_string\n\ndef test_hello():\n    assert reverse_string('hello') == 'olleh'\n\ndef test_empty():\n    assert reverse_string('') == ''\n"
  },
  "setup_commands": ["pip install -q pytest"],
  "verifier": {
    "type": "pytest",
    "command": "python -m pytest -q",
    "success_exit_code": 0,
    "timeout_s": 30
  },
  "metadata": {
    "difficulty": "easy",
    "tags": ["python", "strings"],
    "language": "python"
  }
}
